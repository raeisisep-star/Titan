# 🧪 دستورالعمل تست نهایی Phase 3.4

**تاریخ:** ۱۴۰۴/۰۸/۱۸  
**وضعیت:** ✅ آماده تست با داده‌های واقعی

---

## 🎯 هدف تست

بررسی اینکه سیستم لاگ‌ها **فقط داده‌های واقعی** را نمایش می‌دهد و **هیچ داده تستی** در پروداکشن وجود ندارد.

---

## ⚙️ تغییرات اعمال شده

### 1. Feature Flag سیستم
```bash
LOGS_DEMO=false  # فقط لاگ‌های واقعی (پروداکشن)
```

### 2. فیلتر خودکار
- لاگ‌های تست: ۱۰ عدد (`:00.000Z` تمام می‌شود)
- لاگ‌های واقعی: ۳۴+ عدد (میلی‌ثانیه دارند)
- **در API: فقط واقعی برگردانده می‌شود!**

### 3. فیلد جدید `source`
```json
{
  "level": 30,
  "time": "2025-11-08T12:35:48.364Z",
  "msg": "✅ Redis connected",
  "source": "real"  ← جدید!
}
```

### 4. آمار `byLevel`
```json
{
  "byLevel": {
    "fatal": 0,
    "error": 0,
    "warn": 0,
    "info": 34,
    "debug": 0,
    "trace": 0
  }
}
```

---

## 🚀 مراحل تست (به ترتیب)

### مرحله 1: پاک کردن کش ⚠️

**حتماً انجام دهید!**

```
Ctrl + Shift + R  (ویندوز)
Cmd + Shift + R   (مک)
```

**یا پنجره Incognito:**
```
Ctrl + Shift + N  (Chrome)
Ctrl + Shift + P  (Firefox)
```

---

### مرحله 2: ورود به سیستم

```
URL: https://www.zala.ir
نام کاربری: admin
رمز عبور: [رمز شما]
```

---

### مرحله 3: تست تب سیستم

#### الف) رفتن به تنظیمات
1. کلیک روی ⚙️ **تنظیمات** در sidebar
2. کلیک روی تب **"سیستم"**

#### ب) پیدا کردن بخش لاگ‌ها
3. اسکرول به پایین تا **"📋 لاگ‌های سیستم"**

#### ج) بررسی داده‌ها
✅ **باید ببینید:**
```
✅ Redis connected [۱۴:۳۵:۴۸ ۱۴۰۴/۰۸/۱۸]
✅ Server is running [۱۴:۳۵:۴۸ ۱۴۰۴/۰۸/۱۸]
✅ Login successful: admin [۱۲:۰۲:۱۷ ۱۴۰۴/۰۸/۱۸]
...
```

❌ **نباید ببینید:**
```
❌ Server started successfully [۰۹:۰۰:۰۰]  ← این تسته!
❌ Database connection established [۰۹:۱۵:۰۰]  ← این تسته!
❌ Failed to fetch market data [۰۹:۳۰:۰۰]  ← این تسته!
```

**تشخیص:**
- لاگ واقعی: میلی‌ثانیه دارد `[۱۴:۳۵:۴۸.۳۶۴]`
- لاگ تست: ثانیه دقیق `[۰۹:۰۰:۰۰.۰۰۰]`

#### د) تست دکمه‌ها
4. کلیک روی **"بروزرسانی"**
   - باید لاگ‌ها دوباره لود شوند
   - همان داده‌های واقعی را ببینید

5. کلیک روی **"مشاهده کامل"**
   - باید داشبورد کامل باز شود
   - بدون خطا!

---

### مرحله 4: تست داشبورد کامل

#### الف) بررسی آمار (Stats Cards)
```
📦 حجم کل: ~4 KB
ℹ️  اطلاعات: 34 عدد  ← همه واقعی
⚠️  هشدارها: 0 عدد
❌ خطاها: 0 عدد
```

**قبلاً داشتید:**
```
ℹ️  اطلاعات: 23 عدد
⚠️  هشدارها: 1 عدد  ← تست بود!
❌ خطاها: 1 عدد  ← تست بود!
```

#### ب) تست فیلتر سطح
1. انتخاب **"همه"** → همه لاگ‌های واقعی
2. انتخاب **"اطلاعات"** → فقط INFO
3. انتخاب **"خطاها"** → هیچی (چون خطا نداریم!)

#### ج) تست جستجو
1. جستجو: `Login` → لاگ‌های ورود
2. جستجو: `Redis` → لاگ‌های Redis
3. جستجو: `Server` → لاگ‌های سرور

#### د) تست Auto-Refresh
- منتظر بمانید ۱۰ ثانیه
- باید خودکار بروزرسانی شود
- شماره لاگ‌ها ممکن است تغییر کند (لاگ‌های جدید)

---

### مرحله 5: بررسی Console (F12)

#### الف) باز کردن DevTools
```
F12 (همه مرورگرها)
Ctrl + Shift + I (ویندوز)
Cmd + Option + I (مک)
```

#### ب) رفتن به تب Console

✅ **باید ببینید:**
```javascript
✅ window.systemTab registered
✅ LogsModule registered in TitanModules namespace
📋 Starting Logs Dashboard...
✅ Logs loaded
داشبورد لاگ‌ها بارگذاری شد
```

❌ **نباید ببینید:**
```javascript
❌ Uncaught SyntaxError
❌ window.logsModule.render is not a function
❌ ماژول logs یافت نشد
```

---

### مرحله 6: بررسی Network Tab

#### الف) رفتن به تب Network در DevTools

#### ب) Reload کردن صفحه (F5)

#### ج) جستجو کنید: `app.js`

✅ **باید ببینید:**
```
Name: app.js?v=1762605372912
Status: 200 (نه 304!)
Size: 439 KB (نه from cache!)
```

#### د) جستجو کنید: `logs.js`

✅ **باید ببینید:**
```
Name: logs.js?v=1762605372912
Status: 200
Size: 22 KB
```

---

## 📸 اسکرین‌شات‌های مورد نیاز (4 عدد)

### 1️⃣ System Tab - پیش‌نمایش لاگ‌ها
**محل:**
- Settings → System tab → اسکرول پایین

**باید شامل:**
- کارت "📋 لاگ‌های سیستم"
- ۱۰-۲۰ لاگ واقعی با رنگ
- تاریخ شمسی
- دکمه‌های "بروزرسانی" و "مشاهده کامل"

**بررسی:**
- ✅ هیچ لاگ تستی نباید دیده شود
- ✅ همه تاریخ‌ها میلی‌ثانیه دارند
- ✅ پیام‌ها واقعی هستند (Login, Redis, Server...)

---

### 2️⃣ Full Logs Dashboard
**محل:**
- بعد از کلیک روی "مشاهده کامل"

**باید شامل:**
- آمار بالا (Info: 34, Warn: 0, Error: 0)
- فیلتر سطح
- کادر جستجو
- جدول لاگ‌ها

**بررسی:**
- ✅ تعداد INFO حدود ۳۴ باشد (نه ۲۳!)
- ✅ تعداد WARN و ERROR صفر باشد (قبلاً ۱ و ۱ بود!)
- ✅ همه لاگ‌ها واقعی باشند

---

### 3️⃣ Console - بدون خطا
**محل:**
- F12 → Console tab

**باید شامل:**
- لاگ‌های سبز (✅)
- پیام‌های "loaded" و "registered"
- بدون خطای قرمز

**بررسی:**
- ✅ هیچ `Uncaught` نباشد
- ✅ هیچ `SyntaxError` نباشد
- ✅ هیچ `404 Not Found` نباشد

---

### 4️⃣ Network Tab - نسخه جدید Assets
**محل:**
- F12 → Network tab → Reload (F5)

**باید شامل:**
- `app.js?v=1762605372912`
- `logs.js?v=1762605372912`
- Status: 200 (نه 304)

**بررسی:**
- ✅ نسخه جدید لود شده (۱۷۶۲۶۰۵۳۷۲۹۱۲)
- ✅ از cache نیامده (Size نباید "memory cache" باشد)
- ✅ همه فایل‌ها 200 هستند

---

## ✅ Checklist نهایی

### قبل از گرفتن اسکرین‌شات:

- [ ] کش مرورگر پاک شد (Hard Refresh)
- [ ] وارد سیستم شدم (admin)
- [ ] تب سیستم را باز کردم
- [ ] لاگ‌های واقعی را دیدم (بدون تست!)
- [ ] دکمه "مشاهده کامل" کار می‌کند
- [ ] داشبورد کامل باز شد
- [ ] آمار درست است (Info: ~34, Warn/Error: 0)
- [ ] Console بدون خطا
- [ ] Network Tab نسخه جدید را نشان می‌دهد

### گرفتن اسکرین‌شات‌ها:

- [ ] Screenshot 1: System Tab Preview
- [ ] Screenshot 2: Full Logs Dashboard
- [ ] Screenshot 3: Console Tab
- [ ] Screenshot 4: Network Tab

---

## 🔍 رفع مشکلات احتمالی

### مشکل 1: هنوز لاگ‌های تست می‌بینم
**راه حل:**
```bash
# کش را دوباره پاک کنید
Ctrl + Shift + Delete → Clear cache
# سپس:
Ctrl + Shift + R
```

### مشکل 2: خطای "ماژول logs یافت نشد"
**راه حل:**
```bash
# Incognito window باز کنید
Ctrl + Shift + N
# وارد شوید و تست کنید
```

### مشکل 3: تعداد لاگ‌ها ۲۳ است (نه ۳۴)
**علت:** کش قدیمی
**راه حل:**
```bash
# DevTools را باز نگه دارید
F12
# تب Network → ✅ Disable cache
# Reload کنید
F5
```

---

## 📊 داده‌های انتظاری

### تعداد لاگ‌ها:
```
قبل (با تست): 26 = 16 واقعی + 10 تست
الان (بدون تست): 34 = همه واقعی
```

### آمار byLevel:
```
قبل:
  Info: 23 (8 تست + 15 واقعی)
  Warn: 1 (تست)
  Error: 1 (تست)

الان:
  Info: 34 (همه واقعی)
  Warn: 0
  Error: 0
```

### پیام‌های واقعی:
```
✅ Redis connected
✅ Server is running
✅ Login successful: admin
✅ Login successful: sepehr
🔐 CORS: https://www.zala.ir
📊 Health check: http://...
```

### پیام‌های تست (نباید ببینید!):
```
❌ Server started successfully  ← ۰۹:۰۰:۰۰
❌ Database connection established  ← ۰۹:۱۵:۰۰
❌ Redis connected  ← ۰۹:۲۰:۰۰
❌ External API response slow  ← ۰۹:۲۵:۰۰
❌ Failed to fetch market data  ← ۰۹:۳۰:۰۰
```

---

## 🎉 نتیجه موفقیت

اگر همه موارد بالا را دیدید:

✅ **Feature Flag کار می‌کند!**
✅ **فیلتر تست لاگ‌ها موفق است!**
✅ **فقط داده واقعی در پروداکشن!**
✅ **آماده برای PR و Merge!**

---

**بعد از تست موفق:**
1. ۴ اسکرین‌شات را بگیرید
2. PR را ایجاد کنید
3. مستندات را چک کنید
4. Merge کنید!

**🎊 تبریک! Phase 3.4 تکمیل شد!**
