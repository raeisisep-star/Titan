# 📍 کجا لاگ‌ها را پیدا کنم؟

## ✅ **مکان صحیح لاگ‌ها**

### مرحله 1️⃣: رفتن به تنظیمات
کلیک روی **"تنظیمات"** در منوی بالا (گوشه راست بالا)

### مرحله 2️⃣: انتخاب تب "سیستم"
**⚠️ مهم:** نه تب "عمومی"، بلکه تب **"سیستم"** (System)

از چپ به راست تب‌ها:
1. **عمومی** (General) ← ❌ اینجا نیست!
2. **صرافی** (Exchange)
3. **سیستم** (System) ← ✅ **اینجا است!**
4. بقیه تب‌ها...

### مرحله 3️⃣: اسکرول به پایین در تب سیستم
بعد از انتخاب تب **سیستم**، اسکرول کن پایین تا ببینی:

```
📋 لاگ‌های سیستم
[5 لاگ آخر اینجا نمایش داده می‌شود]
[دکمه: 🔗 مشاهده کامل]
```

### مرحله 4️⃣: کلیک روی "مشاهده کامل"
این دکمه داشبورد کامل لاگ‌ها را باز می‌کند.

---

## 🔍 **چک کنید در Console:**

اگر هنوز ندیدید، در Console این دستور را بزنید:

```javascript
// بررسی اینکه settingsModule متد لاگ‌ها را دارد یا نه
console.log('Has loadSystemLogsPreview?', typeof settingsModule !== 'undefined' && typeof settingsModule.loadSystemLogsPreview === 'function');

// تست دستی بارگذاری لاگ‌ها
if (typeof settingsModule !== 'undefined' && typeof settingsModule.loadSystemLogsPreview === 'function') {
    settingsModule.loadSystemLogsPreview();
}
```

---

## 🐛 **اگر هنوز ندیدید:**

این احتمالات را بررسی کنید:

### احتمال 1: تب سیستم بارگذاری نشده
```javascript
// در Console این را بزنید:
console.log('System tab HTML:', document.querySelector('[data-tab="system"]'));
```

اگر `null` بود، یعنی تب سیستم بارگذاری نشده.

### احتمال 2: المنت لاگ‌ها در DOM نیست
```javascript
// بررسی المنت لاگ‌ها:
console.log('Logs section:', document.querySelector('#system-logs-preview'));
```

### احتمال 3: Error در بارگذاری
```javascript
// بررسی Error ها:
console.error('Check errors above');
```

---

## 📸 **برای من اسکرین‌شات بفرستید:**

لطفاً از این موارد اسکرین‌شات بگیرید:

1. **صفحه تنظیمات** با تب‌های بالا (عمومی، صرافی، **سیستم**)
2. **تب سیستم** بعد از کلیک (محتوای کامل)
3. **Console** (تب Console در DevTools)

---

## ✅ **انتظار می‌رود ببینید:**

در تب **سیستم**، بخش جدید با این محتوا:

```
📋 لاگ‌های سیستم
─────────────────────────────────────

[INFO] 2025-11-02 14:30:15
Server started on port 5000

[WARN] 2025-11-02 14:28:10  
High memory usage detected

[ERROR] 2025-11-02 14:25:30
Database connection failed

[دکمه آبی: 🔗 مشاهده کامل]
```

---

**خلاصه:** برو تنظیمات → تب **سیستم** (نه عمومی!) → اسکرول پایین
