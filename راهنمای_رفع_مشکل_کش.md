# راهنمای رفع مشکل کش مرورگر تایتان

## 🔍 تشخیص مشکل

اگر با این خطا مواجه شدید:
```
Uncaught SyntaxError: The requested module '../core/constants.js' does not provide an export named 'API_ENDPOINTS'
```

یا صفحه به درستی بارگذاری نمی‌شود، مشکل از **کش مرورگر** است.

## ⚡ راه‌حل سریع (پیشنهادی)

### مرحله 1: صفحه پاکسازی خودکار
1. به آدرس زیر بروید:
   ```
   https://your-domain.com/cache-clear-instructions.html
   ```
2. روی دکمه **"پاکسازی خودکار"** کلیک کنید
3. صبر کنید تا پاکسازی انجام شود و به صفحه اصلی برگردید

### مرحله 2: Hard Refresh
اگر مرحله 1 کار نکرد، از این کلیدها استفاده کنید:

- **Windows/Linux**: `Ctrl + Shift + R` یا `Ctrl + F5`
- **Mac**: `Cmd + Shift + R`

## 🛠️ راه‌حل کامل (اگر مشکل ادامه دارد)

### برای گوگل کروم
1. `Ctrl + Shift + Delete` را فشار دهید
2. "بازه زمانی" را روی **"همه زمان‌ها"** تنظیم کنید
3. این موارد را انتخاب کنید:
   - ✅ تصاویر و فایل‌های ذخیره شده
   - ✅ کوکی‌ها و داده‌های سایت
4. روی **"پاک کردن داده‌ها"** کلیک کنید
5. صفحه را با `Ctrl + F5` بارگذاری مجدد کنید

### برای فایرفاکس
1. `Ctrl + Shift + Delete` را فشار دهید
2. "بازه زمانی" را روی **"همه چیز"** تنظیم کنید
3. این موارد را انتخاب کنید:
   - ✅ کش
   - ✅ کوکی‌ها
4. روی **"پاک کردن اکنون"** کلیک کنید
5. صفحه را با `Ctrl + Shift + R` بارگذاری مجدد کنید

## 👨‍💻 برای توسعه‌دهندگان: راه‌حل دائمی

### 1. پیکربندی Nginx
فایل `nginx-cache-fix.conf` را به تنظیمات nginx اضافه کنید:

```bash
# ویرایش فایل پیکربندی
sudo nano /etc/nginx/sites-available/titan

# محتویات nginx-cache-fix.conf را اضافه کنید
# سپس:
sudo nginx -t
sudo systemctl reload nginx
```

### 2. تست کش (Developer Console)
در کنسول مرورگر این کد را اجرا کنید:

```javascript
// بررسی کش‌های موجود
caches.keys().then(keys => console.log('Caches:', keys));

// پاکسازی همه کش‌ها
caches.keys().then(keys => 
  Promise.all(keys.map(k => caches.delete(k)))
).then(() => console.log('✅ All caches cleared'));

// بررسی Service Workers
navigator.serviceWorker.getRegistrations().then(regs => 
  console.log('Service Workers:', regs.length)
);
```

### 3. اضافه کردن Cache-Buster به HTML
فایل `index.html` اکنون شامل اسکریپت خودکار پاکسازی کش است که در اولین بارگذاری اجرا می‌شود.

## 🔄 چرا این مشکل رخ داد؟

1. **ES6 Module Imports**: مرورگرها ماژول‌های JavaScript را به صورت تهاجمی کش می‌کنند
2. **Query Parameters کافی نیست**: `?v=timestamp` برای ES6 imports کار نمی‌کند
3. **Dependency Chain**: وقتی یک ماژول تغییر می‌کند، مرورگر همچنان نسخه قدیمی والد را سرو می‌کند

## ✅ تایید رفع مشکل

بعد از اعمال تغییرات، این موارد را بررسی کنید:

1. **کنسول مرورگر**: نباید خطای مربوط به `constants.js` یا `dashboard-v2` نمایش دهد
2. **Network Tab**: فایل‌های JS باید با header های `Cache-Control: no-cache` بارگذاری شوند
3. **صفحه اصلی**: باید بدون خطا بارگذاری شود

## 📞 پشتیبانی

اگر بعد از انجام این مراحل مشکل ادامه داشت:

1. لاگ کنسول مرورگر را کپی کنید
2. محتویات Network Tab (فیلتر: JS) را بررسی کنید
3. بررسی کنید که آیا nginx configuration به درستی reload شده است:
   ```bash
   sudo nginx -t
   sudo systemctl status nginx
   ```

## 🎯 خلاصه

**برای کاربران عادی**: 
- به `/cache-clear-instructions.html` بروید
- روی دکمه پاکسازی کلیک کنید
- یا `Ctrl + Shift + R` را فشار دهید

**برای توسعه‌دهندگان**:
- nginx configuration را با `nginx-cache-fix.conf` به‌روز کنید
- Service Workers و Cache API را غیرفعال کنید (development)
- از `Disable cache` در DevTools استفاده کنید

---

**تاریخ به‌روزرسانی**: نوامبر 2024
**نسخه**: 2.0 - رفع کامل مشکل کش ES6 Modules
