# 🚀 دستورالعمل پاک‌سازی کش Cloudflare

## ✅ وضعیت فعلی

**همه تغییرات سمت سرور انجام شده است!**

- ✅ فایل‌های صحیح در مسیر درست قرار دارند
- ✅ Nginx از مسیر صحیح فایل‌ها را سرو می‌کند
- ✅ هدرهای Cache-Control برای index.html تنظیم شده (no-cache)
- ✅ ماژول logs.js ایجاد و ثبت شده است
- ✅ تنظیمات → سیستم: بخش لاگ‌ها اضافه شده

## 🔴 تنها مشکل باقی‌مانده: کش Cloudflare

Cloudflare نسخه قدیمی index.html را در کش دارد. باید کش را پاک کنید.

---

## 📋 روش 1: پاک‌سازی کامل کش (توصیه می‌شود - سریع‌ترین)

### مراحل:

1. به داشبورد Cloudflare بروید:

   ```
   https://dash.cloudflare.com/
   ```

2. دامنه `zala.ir` را انتخاب کنید

3. از منوی سمت چپ، به این مسیر بروید:

   ```
   Caching → Configuration
   ```

4. دکمه **"Purge Everything"** را پیدا کنید و کلیک کنید

5. تأیید کنید (Confirm)

6. **صبر کنید 30-60 ثانیه** تا کش پاک شود

---

## 📋 روش 2: پاک‌سازی انتخابی URL (دقیق‌تر)

### مراحل:

1. در داشبورد Cloudflare: `Caching → Configuration`

2. دکمه **"Purge Cache"** → **"Custom Purge"** را بزنید

3. این URLها را اضافه کنید (هر کدام در یک خط):

   ```
   https://www.zala.ir/
   https://www.zala.ir/index.html
   https://zala.ir/
   https://zala.ir/index.html
   ```

4. **"Purge"** را بزنید

---

## ✅ بعد از پاک‌سازی کش: تست در مرورگر

### مرحله 1: باز کردن حالت Incognito/Private

- **Chrome/Edge**: `Ctrl + Shift + N`
- **Firefox**: `Ctrl + Shift + P`
- **Safari**: `Cmd + Shift + N`

### مرحله 2: بازدید از سایت

```
https://www.zala.ir/
```

### مرحله 3: Hard Refresh (رفرش کامل)

- **Windows**: `Ctrl + Shift + R`
- **Mac**: `Cmd + Shift + R`

### مرحله 4: باز کردن DevTools

- زدن کلید `F12`
- رفتن به تب **Network**
- رفرش مجدد صفحه (`Ctrl+Shift+R`)
- پیدا کردن فایل: `logs.js?v=202511021336`
- بررسی اینکه وضعیت 200 است

---

## 🔍 تست کارکرد ویژگی Logs

### 1. لاگین به داشبورد

با حساب کاربری خود وارد شوید.

### 2. رفتن به تنظیمات → سیستم

- کلیک روی **"تنظیمات"** در منوی بالا
- انتخاب تب **"سیستم"**
- اسکرول به پایین تا بخش **"📋 لاگ‌های سیستم"**

### 3. بررسی پیش‌نمایش لاگ‌ها

باید ببینید:

- ✅ 5 لاگ آخر نمایش داده می‌شود
- ✅ رنگ‌بندی بر اساس سطح (آبی=info، زرد=warn، قرمز=error)
- ✅ دکمه **"مشاهده کامل"** در سمت راست

### 4. باز کردن داشبورد کامل لاگ‌ها

- کلیک روی **"مشاهده کامل"**
- داشبورد جدید باز می‌شود با:
  - ✅ فیلترهای سطح لاگ (همه، خطا، هشدار، اطلاعات)
  - ✅ جعبه جستجو
  - ✅ انتخاب تعداد لاگ‌ها (50، 100، 200، 500)
  - ✅ دکمه رفرش دستی
  - ✅ سوییچ رفرش خودکار (هر 10 ثانیه)
  - ✅ جدول لاگ‌ها با زمان، سطح، پیام، و جزئیات

---

## 📸 آماده برای اسکرین‌شات

بعد از اینکه تغییرات را دیدید:

### اسکرین‌شات 1: تنظیمات → سیستم

- بخش پیش‌نمایش لاگ‌های سیستم
- 5 لاگ آخر
- دکمه "مشاهده کامل"

### اسکرین‌شات 2: داشبورد کامل لاگ‌ها

- فیلترها و کنترل‌ها
- جدول لاگ‌ها
- رنگ‌بندی سطوح

### اسکرین‌شات 3: DevTools → Network

- فایل logs.js لود شده
- هدر Cache-Control
- پارامتر version: ?v=202511021336

---

## 🐛 اگر هنوز تغییرات را نمی‌بینید

### 1. پاک کردن کش مرورگر به صورت دستی

**Chrome/Edge**:

- `Settings` → `Privacy and security` → `Clear browsing data`
- انتخاب: `Cached images and files`
- Time range: `All time`
- کلیک `Clear data`

**Firefox**:

- `Settings` → `Privacy & Security` → `Cookies and Site Data`
- کلیک `Clear Data`
- انتخاب: `Cached Web Content`
- کلیک `Clear`

### 2. غیرفعال کردن Cache در DevTools

- باز کردن DevTools (F12)
- رفتن به تب Network
- چک کردن گزینه: **"Disable cache"**
- رفرش صفحه

### 3. تست با دستگاه/شبکه دیگر

- تست با موبایل (خاموش کردن WiFi، استفاده از 4G)
- تست با مرورگر دیگر
- تست با کامپیوتر دیگر

### 4. بررسی وضعیت Cloudflare

از داشبورد Cloudflare:

- برو به: `Analytics & Logs` → `Cache Analytics`
- بررسی کن که Purge موفقیت‌آمیز بوده باشد

---

## 📞 در صورت نیاز به کمک

اگر بعد از همه این مراحل هنوز مشکل دارید:

1. اسکرین‌شات از DevTools → Network بگیرید
2. اسکرین‌شات از DevTools → Console بگیرید
3. خروجی این دستور را بدهید:
   ```bash
   curl -sI https://www.zala.ir/ | grep -iE "cache|x-titan"
   ```

---

## ⏱️ زمان تخمینی

- پاک‌سازی کش Cloudflare: 30-60 ثانیه
- مشاهده تغییرات در مرورگر: فوری (با incognito + hard refresh)
- **مجموع**: کمتر از 2 دقیقه

---

## ✨ نتیجه نهایی

بعد از پاک‌سازی کش، باید:

- ✅ لاگ‌ها در تنظیمات → سیستم نمایش داده شوند
- ✅ دکمه "مشاهده کامل" داشبورد کامل لاگ‌ها را باز کند
- ✅ همه قابلیت‌های فیلتر، جستجو، و رفرش خودکار کار کنند

**موفق باشید! 🎉**
