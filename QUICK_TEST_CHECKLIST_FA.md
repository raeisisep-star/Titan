# چک‌لیست تست سریع ادغام AI

## 🎯 هدف
تأیید اینکه ادغام AI Tab به درستی کار می‌کند و هیچ خطایی وجود ندارد.

## ✅ مراحل تست Frontend

### 1. تست اولیه
```bash
# باز کردن صفحه با Hard Refresh
Ctrl + Shift + R  (Windows/Linux)
Cmd + Shift + R   (Mac)
```

### 2. بررسی Console
باز کردن Developer Tools (F12) و بررسی Console:

**باید این پیام‌ها نمایش داده شوند:**
```
✅ TITAN AI API module loaded
✅ TITAN AI Adapters module loaded
🔧 Applying AI Tab Integration Patches...
✅ AI Tab Integration Patches Applied Successfully
```

**اگر این پیام‌ها نمایش داده نشد:**
- ترتیب اسکریپت‌ها در index.html را بررسی کنید
- Cache مرورگر را پاک کنید
- Hard Refresh دوباره انجام دهید

### 3. تست در Console

```javascript
// تست API برای ایجنت 1 (باید available: true برگرداند)
await window.TITAN_AI_API.fetchAgentBlock(1)

// تست API برای ایجنت 5 (باید available: false برگرداند)
await window.TITAN_AI_API.fetchAgentBlock(5)

// تست Adapter برای مقدار null (نباید TypeError بدهد)
window.TITAN_AI_ADAPTERS.adaptAgentStatus(1, null)

// تست Safe Rendering برای undefined
window.TITAN_AI_ADAPTERS.safeFormatNumber(undefined)
// باید برگرداند: "N/A"
```

### 4. تست UI

**مسیر:** تنظیمات → تب AI

**تست ایجنت‌های 1-4 و 11:**
- [ ] کلیک روی Agent 01 → باید Modal با اطلاعات تکنیکال نمایش داده شود
- [ ] کلیک روی Agent 02 → باید Modal با اطلاعات Order Book نمایش داده شود
- [ ] کلیک روی Agent 03 → باید Modal با اطلاعات News Sentiment نمایش داده شود
- [ ] کلیک روی Agent 04 → باید Modal با اطلاعات Market Microstructure نمایش داده شود
- [ ] کلیک روی Agent 11 → باید Modal با اطلاعات Multi-Timeframe نمایش داده شود
- [ ] بررسی Console: نباید هیچ TypeError وجود داشته باشد

**تست ایجنت‌های 5-10:**
- [ ] کلیک روی Agent 05 → باید Modal با پیام "🚧 Coming Soon" نمایش داده شود
- [ ] کلیک روی Agent 06 → باید Modal با پیام "🚧 Coming Soon" نمایش داده شود
- [ ] کلیک روی Agent 07 → باید Modal با پیام "🚧 Coming Soon" نمایش داده شود
- [ ] کلیک روی Agent 08 → باید Modal با پیام "🚧 Coming Soon" نمایش داده شود
- [ ] کلیک روی Agent 09 → باید Modal با پیام "🚧 Coming Soon" نمایش داده شود
- [ ] کلیک روی Agent 10 → باید Modal با پیام "🚧 Coming Soon" نمایش داده شود
- [ ] بررسی Console: نباید خطای 404 خام (raw) نمایش داده شود

**تست ایجنت‌های 12-15:**
- [ ] کلیک روی Agent 12 → رفتار اصلی بدون تغییر
- [ ] کلیک روی Agent 13 → رفتار اصلی بدون تغییر
- [ ] کلیک روی Agent 14 → رفتار اصلی بدون تغییر
- [ ] کلیک روی Agent 15 → رفتار اصلی بدون تغییر

## ✅ مراحل تست Backend

**پیش‌نیاز:** پیاده‌سازی Endpoint‌های Backend برای ایجنت‌های 5-10

### 1. تست با curl

```bash
# تست Health Check
curl http://localhost:3000/api/health

# تست Agent 5
curl http://localhost:3000/api/ai/agents/5/status
curl http://localhost:3000/api/ai/agents/5/config
curl http://localhost:3000/api/ai/agents/5/history

# تست Agent 1 (برای مقایسه)
curl http://localhost:3000/api/ai/agents/1/status
```

### 2. تأیید Response

**برای ایجنت‌های 5-10 (Not Available):**
```json
{
  "agentId": "agent-05",
  "installed": false,
  "available": false,
  "message": "This agent is not yet implemented"
}
```

**برای ایجنت‌های 1-4 و 11 (Available):**
```json
{
  "agentId": "agent-01",
  "installed": true,
  "available": true,
  "status": "active",
  "health": "good",
  ...
}
```

### 3. بررسی HTTP Status Code

```bash
# باید همیشه 200 OK برگرداند (حتی برای ایجنت‌های Not Available)
curl -I http://localhost:3000/api/ai/agents/5/status | grep "HTTP"
# باید نتیجه دهد: HTTP/1.1 200 OK

# نباید 404 Not Found برگرداند
curl -I http://localhost:3000/api/ai/agents/5/status | grep "404"
# باید خالی باشد (بدون نتیجه)
```

## 📊 معیارهای قبولی (Acceptance Criteria)

| معیار | نتیجه مورد انتظار | وضعیت |
|-------|-------------------|--------|
| No TypeError برای ایجنت‌های 1-4 و 11 | هیچ خطایی در Console | ⬜ |
| Coming Soon برای ایجنت‌های 5-10 | Modal با پیام "🚧 Coming Soon" | ⬜ |
| عدم تغییر در ایجنت‌های 12-15 | رفتار اصلی حفظ شود | ⬜ |
| عدم 404 خام در Console | همه 404ها به صورت graceful مدیریت شوند | ⬜ |

## 🐛 اشکالات متداول

### خطا: "Module not found" در Console
**راه‌حل:**
1. بررسی ترتیب اسکریپت‌ها در index.html
2. Hard Refresh انجام دهید
3. Network Tab را بررسی کنید

### خطا: TypeError هنوز وجود دارد
**راه‌حل:**
1. Hard Refresh مرورگر
2. بررسی کنید که پیام "✅ AI Tab Integration Patches Applied" نمایش داده شده
3. تأیید کنید ai-tab-integration.js بارگذاری شده است

### خطا: Modal Coming Soon نمایش داده نمی‌شود
**راه‌حل:**
1. بررسی پیاده‌سازی Backend
2. تأیید کنید Endpoint‌ها HTTP 200 با `{available: false}` برمی‌گردانند
3. تست با curl

## 📝 گزارش نتایج تست

پس از اتمام تست‌ها، نتایج را ثبت کنید:

```
تاریخ تست: ____________________
تست‌کننده: ____________________

✅ Frontend:
  - Console Logs: ⬜ موفق / ⬜ ناموفق
  - UI Agents 1-4,11: ⬜ موفق / ⬜ ناموفق
  - UI Agents 5-10: ⬜ موفق / ⬜ ناموفق
  - UI Agents 12-15: ⬜ موفق / ⬜ ناموفق

✅ Backend:
  - Endpoints Status: ⬜ موفق / ⬜ ناموفق
  - Response Format: ⬜ موفق / ⬜ ناموفق
  - HTTP 200: ⬜ موفق / ⬜ ناموفق

نتیجه کلی: ⬜ موفق / ⬜ نیاز به اصلاح

یادداشت‌ها:
_________________________________________________
_________________________________________________
```

## 🚀 مرحله بعدی

اگر همه تست‌ها موفق بودند:
- ✅ Frontend آماده Deploy است
- ⏳ Backend باید Endpoint‌ها را پیاده‌سازی کند
- ⏳ پس از پیاده‌سازی Backend، تست دوباره انجام شود
- ✅ Deploy به Production

اگر تست‌ها ناموفق بودند:
- 🐛 بررسی اشکالات متداول
- 📚 مراجعه به BACKEND_INTEGRATION_GUIDE.md
- 💬 تماس با تیم Development
