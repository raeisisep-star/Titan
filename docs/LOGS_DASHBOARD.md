# 📊 Phase 3.3: Logs Dashboard & Monitoring System

**تاریخ**: 2025-11-02  
**نسخه**: 1.0.0  
**وضعیت**: ✅ مستقر و فعال

---

## 📋 فهرست مطالب

1. [نمای کلی](#نمای-کلی)
2. [معماری سیستم](#معماری-سیستم)
3. [API Endpoints](#api-endpoints)
4. [رابط کاربری](#رابط-کاربری)
5. [امنیت](#امنیت)
6. [استفاده](#استفاده)
7. [عیب‌یابی](#عیب-یابی)
8. [بهترین شیوه‌ها](#بهترین-شیوه-ها)

---

## 🎯 نمای کلی

سیستم داشبورد لاگ‌ها یک رابط وب کامل برای مشاهده، فیلتر و تحلیل لاگ‌های real-time سرور TITAN است.

### ✨ قابلیت‌های کلیدی

✅ **مشاهده Real-time**: بروزرسانی خودکار هر 10 ثانیه  
✅ **فیلتر پیشرفته**: بر اساس سطح (level) و جست‌وجوی متنی  
✅ **نمایش رنگی**: رنگ‌بندی براساس سطح لاگ (Error قرمز، Warning نارنجی، Info آبی)  
✅ **آمار زنده**: تعداد خطاها، هشدارها، و حجم فایل‌ها  
✅ **لاگ‌های خطا**: نمایش جداگانه خطاهای ثبت‌شده توسط error-watch  
✅ **رابط کاربری ریسپانسیو**: سازگار با موبایل و دسکتاپ  

---

## 🏗️ معماری سیستم

```
┌─────────────────────────────────────────────────────────────┐
│                   TITAN Logs Dashboard                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────┐      ┌──────────────┐      ┌───────────┐ │
│  │   Browser    │◄────►│  API Server  │◄────►│ Log Files │ │
│  │  (Frontend)  │      │  (Backend)   │      │           │ │
│  │              │      │              │      │           │ │
│  │ logs-        │      │ server.js    │      │ titan.log │ │
│  │ dashboard.js │      │              │      │ error-    │ │
│  │              │      │              │      │ alerts.   │ │
│  │              │      │              │      │ log       │ │
│  └──────────────┘      └──────────────┘      └───────────┘ │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

### مؤلفه‌های سیستم

1. **Backend API** (`server.js`)
   - `/api/logs/recent` - دریافت آخرین لاگ‌ها
   - `/api/logs/errors` - دریافت لاگ‌های خطا
   - `/api/logs/stats` - آمار کلی سیستم

2. **Frontend Module** (`public/static/modules/logs-dashboard.js`)
   - مدیریت UI و رندرینگ
   - فیلترینگ و جست‌وجو
   - بروزرسانی خودکار (auto-refresh)

3. **HTML Page** (`public/logs-dashboard.html`)
   - صفحه اصلی داشبورد
   - احراز هویت کاربر
   - Navigation و Layout

4. **Log Files**
   - `logs/titan.log` - لاگ‌های اصلی سیستم (JSON format)
   - `logs/error-alerts.log` - هشدارهای خطا از error-watch

---

## 🔌 API Endpoints

### 1. GET /api/logs/recent

دریافت آخرین لاگ‌های سیستم با قابلیت فیلتر و جست‌وجو.

**Query Parameters:**

```
limit:  تعداد لاگ‌ها (default: 100, max: 1000)
level:  فیلتر سطح (all, fatal, error, warn, info, debug, trace)
search: جست‌وجو در پیام‌ها (optional)
```

**مثال درخواست:**

```bash
curl "http://localhost:5000/api/logs/recent?limit=50&level=error"
```

**پاسخ نمونه:**

```json
{
  "success": true,
  "data": {
    "logs": [
      {
        "level": 50,
        "time": "2025-11-02T12:00:00.000Z",
        "service": "titan-backend",
        "env": "production",
        "msg": "Database connection failed"
      }
    ],
    "count": 1,
    "filters": {
      "limit": 50,
      "level": "error",
      "search": null
    },
    "timestamp": "2025-11-02T12:00:01.000Z"
  }
}
```

---

### 2. GET /api/logs/errors

دریافت لاگ‌های خطا از `error-alerts.log`.

**Query Parameters:**

```
limit: تعداد خطاها (default: 50, max: 500)
```

**مثال درخواست:**

```bash
curl "http://localhost:5000/api/logs/errors?limit=10"
```

**پاسخ نمونه:**

```json
{
  "success": true,
  "data": {
    "errors": [
      {
        "id": 0,
        "timestamp": "2025-11-02T12:00:00",
        "message": "[2025-11-02 12:00:00] ⚠️ ERROR in TITAN Backend: Connection timeout",
        "severity": "error"
      }
    ],
    "count": 1,
    "timestamp": "2025-11-02T12:00:01.000Z"
  }
}
```

---

### 3. GET /api/logs/stats

دریافت آمار کلی لاگ‌ها و وضعیت فایل‌ها.

**مثال درخواست:**

```bash
curl "http://localhost:5000/api/logs/stats"
```

**پاسخ نمونه:**

```json
{
  "success": true,
  "data": {
    "total": 500,
    "byLevel": {
      "fatal": 0,
      "error": 5,
      "warn": 12,
      "info": 480,
      "debug": 3,
      "trace": 0
    },
    "files": {
      "main": {
        "path": "logs/titan.log",
        "size": "7.5 KB",
        "lastModified": "2025-11-02T12:00:00.000Z"
      },
      "errors": {
        "path": "logs/error-alerts.log",
        "size": "1.2 KB",
        "lastModified": "2025-11-02T11:55:00.000Z"
      }
    },
    "timestamp": "2025-11-02T12:00:01.000Z"
  }
}
```

---

## 🎨 رابط کاربری

### دسترسی به داشبورد

```
URL: https://www.zala.ir/public/logs-dashboard.html
یا
URL: http://188.40.209.82:5000/public/logs-dashboard.html
```

**نیازمندی**: کاربر باید وارد سیستم شده باشد (JWT token در localStorage)

### اجزای UI

#### 1. **Header Section**
- لوگو و عنوان
- دکمه بروزرسانی (Refresh)
- دکمه توقف/شروع بروزرسانی خودکار
- دکمه بازگشت به داشبورد اصلی
- دکمه خروج (Logout)

#### 2. **Stats Cards**
```
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│  خطاها   │ │ هشدارها  │ │ اطلاعات  │ │حجم فایل │
│    0     │ │    0     │ │   56     │ │  7.5 KB  │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
```

#### 3. **Filters Section**
- **سطح لاگ**: dropdown (all, fatal, error, warn, info, debug, trace)
- **جست‌وجو**: text input با debounce (500ms)
- **تعداد نمایش**: dropdown (50, 100, 200, 500)
- **آخرین بروزرسانی**: timestamp به فارسی

#### 4. **Tabs**
- **لاگ‌های اخیر**: نمایش لاگ‌ها با رنگ‌بندی
- **خطاها**: نمایش خطاهای ثبت‌شده با badge تعداد

#### 5. **Logs Display**
هر لاگ شامل:
- **آیکون**: emoji بر اساس سطح (💀 Fatal, ❌ Error, ⚠️ Warning, ℹ️ Info, 🐛 Debug, 🔍 Trace)
- **Badge سطح**: رنگ‌بندی شده (قرمز، نارنجی، آبی، بنفش، خاکستری)
- **سرویس**: نام سرویس (e.g., titan-backend)
- **زمان**: ساعت:دقیقه:ثانیه به فارسی
- **پیام**: متن لاگ
- **خطا** (اگر وجود داشته باشد): پیام خطا با رنگ قرمز

---

## 🔐 امنیت

### احراز هویت

```javascript
// بررسی توکن در localStorage یا sessionStorage
const token = localStorage.getItem('token') || sessionStorage.getItem('token');

// ارسال توکن در هدر Authorization
headers: {
  'Authorization': `Bearer ${token}`
}
```

**توجه**: در حال حاضر authentication چک نمی‌شود اما header باید ارسال شود. برای production باید JWT validation اضافه شود.

### دسترسی Admin

در نسخه‌های بعدی، فقط کاربران با نقش `admin` می‌توانند به این داشبورد دسترسی داشته باشند.

---

## 📖 استفاده

### راه‌اندازی اولیه

1. **ورود به سیستم**:
```bash
# وارد شوید و token را در localStorage ذخیره کنید
localStorage.setItem('token', 'your-jwt-token');
```

2. **دسترسی به داشبورد**:
```
https://www.zala.ir/public/logs-dashboard.html
```

3. **مشاهده لاگ‌ها**:
   - داشبورد به‌طور خودکار لاگ‌ها را بارگذاری می‌کند
   - بروزرسانی خودکار هر 10 ثانیه فعال است

### استفاده از فیلترها

#### فیلتر بر اساس سطح:
```javascript
// انتخاب سطح از dropdown
document.getElementById('levelFilter').value = 'error';
```

#### جست‌وجو در لاگ‌ها:
```javascript
// تایپ در search input
document.getElementById('searchInput').value = 'database';
// بعد از 500ms خودکار فیلتر می‌شود
```

#### تغییر تعداد نمایش:
```javascript
// انتخاب تعداد از dropdown
document.getElementById('limitFilter').value = '200';
```

### کنترل بروزرسانی خودکار

```javascript
// توقف بروزرسانی خودکار
window.logsDashboard.toggleAutoRefresh();

// بروزرسانی دستی
window.logsDashboard.handleRefresh();
```

### دسترسی Programmatic

```javascript
// دسترسی به instance
const dashboard = window.logsDashboard;

// دریافت لاگ‌های فعلی
console.log(dashboard.logs);

// دریافت خطاها
console.log(dashboard.errors);

// دریافت آمار
console.log(dashboard.stats);
```

---

## 🔧 عیب‌یابی

### مشکل 1: داشبورد بارگذاری نمی‌شود

**علل احتمالی**:
- توکن JWT موجود نیست
- سرور در حال اجرا نیست
- فایل logs-dashboard.js لود نشده

**راه‌حل**:
```bash
# بررسی توکن
localStorage.getItem('token')

# بررسی سرور
curl http://localhost:5000/health

# بررسی لاگ‌های PM2
pm2 logs titan-backend --lines 50
```

---

### مشکل 2: لاگ‌ها نمایش داده نمی‌شوند

**علل احتمالی**:
- فایل لاگ وجود ندارد
- مجوز خواندن فایل ندارید
- فرمت لاگ اشتباه است

**راه‌حل**:
```bash
# بررسی وجود فایل
ls -la /home/ubuntu/Titan/logs/

# بررسی محتوای فایل
tail -20 /home/ubuntu/Titan/logs/titan.log

# بررسی مجوزها
ls -l /home/ubuntu/Titan/logs/titan.log
# باید: -rw-rw-r-- ubuntu ubuntu
```

---

### مشکل 3: خطای "Authentication required"

**علت**: توکن JWT ارسال نشده یا منقضی شده

**راه‌حل**:
```javascript
// ورود مجدد و دریافت توکن جدید
// یا
localStorage.setItem('token', 'new-valid-token');

// سپس صفحه را رفرش کنید
location.reload();
```

---

### مشکل 4: بروزرسانی خودکار کار نمی‌کند

**علت**: interval متوقف شده یا خطا در fetch

**راه‌حل**:
```javascript
// بررسی وضعیت auto-refresh
console.log(window.logsDashboard.isAutoRefreshEnabled);

// راه‌اندازی مجدد
window.logsDashboard.startAutoRefresh();

// بررسی console برای خطاها
// F12 > Console
```

---

## ✅ بهترین شیوه‌ها

### 1. مدیریت حجم لاگ

```bash
# استفاده از logrotate برای مدیریت خودکار (از Phase 3.2)
sudo logrotate -f /etc/logrotate.d/titan

# یا پاک‌سازی دستی لاگ‌های قدیمی
find /home/ubuntu/Titan/logs -name "*.log-*" -mtime +7 -delete
```

### 2. مانیتورینگ Performance

```javascript
// استفاده از limit مناسب برای جلوگیری از بار زیاد
// برای dashboard روزانه: limit=100
// برای تحلیل عمیق: limit=500

// خاموش کردن auto-refresh هنگام تحلیل
window.logsDashboard.stopAutoRefresh();
```

### 3. امنیت

```javascript
// همیشه از HTTPS استفاده کنید در production
// احراز هویت را فعال کنید
// دسترسی را به Admin محدود کنید

// مثال: بررسی نقش کاربر
if (user.role !== 'admin') {
  alert('شما مجوز دسترسی به این صفحه را ندارید');
  window.location.href = '/';
}
```

### 4. بهینه‌سازی

```javascript
// استفاده از debounce برای search (پیاده‌سازی شده)
// کش کردن نتایج برای کاهش بار سرور
// استفاده از pagination برای لاگ‌های زیاد
```

---

## 📈 نمونه Use Cases

### Use Case 1: پیدا کردن خطاهای اخیر

```
1. وارد داشبورد شوید
2. از dropdown "سطح لاگ" گزینه "Error" را انتخاب کنید
3. یا به تب "خطاها" بروید
4. خطاها به ترتیب زمانی نمایش داده می‌شوند
```

### Use Case 2: جست‌وجوی لاگ خاص

```
1. در فیلد "جست‌وجو" کلیدواژه مورد نظر را تایپ کنید (e.g., "database")
2. بعد از 500ms نتایج فیلتر می‌شوند
3. می‌توانید سطح را هم محدود کنید (e.g., error + database)
```

### Use Case 3: مانیتورینگ Real-time

```
1. بروزرسانی خودکار را فعال نگه دارید
2. سطح را روی "all" تنظیم کنید
3. هر 10 ثانیه لاگ‌های جدید نمایش داده می‌شوند
4. برای توقف موقت دکمه "توقف خودکار" را بزنید
```

### Use Case 4: تحلیل آماری

```
1. به Stats Cards نگاه کنید:
   - تعداد خطاها (اگر > 0 باشد، بررسی کنید)
   - تعداد هشدارها (اگر زیاد باشد، نیاز به بررسی دارد)
   - حجم فایل لاگ (اگر بیش از 50MB باشد، logrotate اجرا کنید)
2. برای جزئیات بیشتر از /api/logs/stats استفاده کنید
```

---

## 🚀 توسعه‌های آینده

- [ ] اضافه کردن Authentication واقعی با JWT decode
- [ ] محدود کردن دسترسی به Admin users only
- [ ] اضافه کردن Export به CSV/JSON
- [ ] اضافه کردن Date Range Filter
- [ ] اضافه کردن Chart برای تحلیل روند
- [ ] اضافه کردن WebSocket برای Real-time Streaming
- [ ] اضافه کردن Alerting Rules (email/Telegram)

---

## 📞 پشتیبانی

برای گزارش مشکلات یا سوالات:
- **GitHub Issues**: [raeisisep-star/Titan](https://github.com/raeisisep-star/Titan/issues)
- **سرور**: 188.40.209.82
- **لاگ‌های سیستم**: `/home/ubuntu/Titan/logs/`

---

**آخرین بروزرسانی**: 2025-11-02  
**نسخه مستندات**: 1.0.0  
**نویسنده**: TITAN Development Team
